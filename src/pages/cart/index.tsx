import CartProducts from "../../components/CartProducts";

const Cart = () => {
  return (
    <div className="min-h-screen flex flex-col bg-red-100">
      {/* Main Section */}
      <CartProducts />
    </div>
  );
};

export default Cart;
